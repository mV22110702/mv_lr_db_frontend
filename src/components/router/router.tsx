import {Route} from "react-router";
import {RouterProvider} from "@/components/router-provider.tsx";

export const Router = () => {
    return (
        <RouterProvider>

        </RouterProvider>
    );
};
